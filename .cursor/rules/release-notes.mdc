---
description: GitHub 發行說明格式
alwaysApply: true
---

GitHub Releases 說明一律用萬用打包神器同一套排版（一般字級，不要變超大標題）。

**一定要在 `🚀` 那一行和 `====` 中間空一行。** 若兩行黏在一起，GitHub 會把上一行渲染成巨大 H1。

```
🚀 vX.Y.Z 短標

================================================================================

✨ 核心更新：
[功能] 名稱：說明。

[最佳化] 名稱：說明。

================================================================================

💡 使用須知：
其餘與 v上一版 相同。本機：python app.py。FFmpeg 請放 PATH 或與程式同一層。
```

規則：

- 標題列可維持 `vX.Y.Z 短標`。
- 核心更新每條以 `[分類]` 開頭；條與條之間空一行。
- 分類用：功能、最佳化、設定、介面、修正、新增、優化、精簡。
- 使用須知後面不必再加 `====`。
- 雲端 Agent 建發行頁會顯示 cursor[bot]；若要 `cccsdxtw released this`，只推 tag、由 PC 端 Cursor 或 GitHub 網頁建立發行。
