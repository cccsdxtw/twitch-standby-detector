---
description: GitHub 發行說明格式
alwaysApply: true
---

GitHub Releases 說明一律用這個格式（標題列可維持 `vX.Y.Z 短標`）：

```
🚀 vX.Y.Z 短標
================================================================================
✨ 核心更新：
[功能] 名稱：說明。
[最佳化] 名稱：說明。
[設定] 名稱：說明。
[介面] 名稱：說明。
================================================================================
💡 使用須知：
其餘與 v上一版 相同。本機：python app.py。FFmpeg 請放 PATH 或與程式同一層。
================================================================================
```

規則：

- 開頭一行 `🚀` + 版本 + 短標，接著一整行 `=`。
- 核心更新每條以 `[分類]` 開頭，分類用：功能、最佳化、設定、介面、修正。
- `💡 使用須知` 寫延續哪一版、怎麼跑。
- 雲端 Agent 建發行頁會顯示 cursor[bot]；若要 `cccsdxtw released this`，只推 tag、由 PC 端 Cursor 或 GitHub 網頁建立發行。
